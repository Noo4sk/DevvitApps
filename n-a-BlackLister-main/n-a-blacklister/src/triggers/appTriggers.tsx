import { Devvit } from '@devvit/public-api';

Devvit.addTrigger({
    event: 'AppInstall',
    onEvent: async (event, context) => {
    },
});
  
Devvit.addTrigger({
    event: 'AppUpgrade',
    onEvent: async (event, context) => {

    },
});
